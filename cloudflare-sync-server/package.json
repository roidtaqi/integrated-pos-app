{
  "name": "kastur-cloudflare-sync-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
